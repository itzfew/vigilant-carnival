 services:
  - type: web
    name: youtube-ffmpeg-livestream
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: YOUTUBE_RTMP_URL
        sync: false # Set this in Render dashboard
      - key: NODE_ENV
        value: production
